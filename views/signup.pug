extends layout

block content
  h1 Google express
  if user
    div Welcome #{user.name}

    form(method="POST" action="/auth/logout")
      input(type="submit" value="Logout")
  else
    a(href="/auth/google") Please log in